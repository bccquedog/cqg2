{
  "name": "functions",
  "scripts": {
    "lint": "eslint --ext .ts --ignore-pattern 'lib/**' --ignore-pattern '**/*.js' .",
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "seed": "ts-node --transpile-only scripts/seed.ts",
    "seed:build": "pnpm build && node lib/scripts/seed.js",
    "seed:dev": "pnpm seed",
    "seed:watch": "tsc --watch",
    "clear": "pnpm build && node lib/scripts/clear.js",
    "clear:dev": "pnpm clear",
    "sanity:features": "ts-node --transpile-only scripts/sanityFeatures.ts",
    "test:history": "ts-node --transpile-only scripts/testHistory.ts",
    "seed:clans": "ts-node --transpile-only scripts/seeder.ts",
    "sanity:clans": "ts-node --transpile-only scripts/sanityClans.ts",
    "sanity:matches": "ts-node --transpile-only scripts/sanityMatches.ts",
    "sanity:leagues": "ts-node --transpile-only scripts/sanityLeagues.ts",
    "sanity:tournaments": "ts-node --transpile-only scripts/sanityTournaments.ts",
    "sanity:competitions": "ts-node --transpile-only scripts/sanityCompetitions.ts",
    "seed:events": "ts-node --transpile-only scripts/seedEvents.ts",
    "sanity:events": "ts-node --transpile-only scripts/sanityEvents.ts",
    "seed:players": "ts-node --transpile-only scripts/seedPlayers.ts",
    "sanity:players": "ts-node --transpile-only scripts/sanityPlayers.ts",
    "seed:presence": "ts-node --transpile-only scripts/seedPresence.ts",
    "sanity:presence": "ts-node --transpile-only scripts/sanityPresence.ts",
    "seed:voiceSessions": "ts-node --transpile-only scripts/seedVoiceSessions.ts",
    "sanity:voiceSessions": "ts-node --transpile-only scripts/sanityVoiceSessions.ts",
    "seed:clans": "ts-node --transpile-only scripts/seedClans.ts",
    "sanity:clans": "ts-node --transpile-only scripts/sanityClans.ts",
    "sanity:memberships": "ts-node --transpile-only scripts/sanityMemberships.ts",
    "sanity:adminMemberships": "ts-node --transpile-only scripts/sanityAdminMemberships.ts",
    "sanity:access": "ts-node --transpile-only scripts/sanityAccess.ts",
    "sanity:tickets": "ts-node --transpile-only scripts/sanityTickets.ts",
    "test:tickets": "ts-node --transpile-only scripts/testTickets.ts",
    "example:tickets": "ts-node --transpile-only scripts/exampleTickets.ts",
    "import:tickets": "ts-node --transpile-only scripts/importTicketsExample.ts",
    "sanity:brackets": "ts-node --transpile-only scripts/sanityBrackets.ts",
    "example:brackets": "ts-node --transpile-only scripts/exampleBrackets.ts",
    "test:submitScore": "ts-node --transpile-only scripts/testSubmitScore.ts",
    "issue:bracketTickets": "ts-node --transpile-only scripts/issueBracketTickets.ts",
    "sanity:schedules": "ts-node --transpile-only scripts/sanitySchedules.ts",
    "test:reminders": "ts-node --transpile-only scripts/testReminders.ts",
    "sanity:reminders": "ts-node --transpile-only scripts/sanityReminders.ts",
    "test:wrapReport": "ts-node --transpile-only scripts/testWrapReport.ts",
    "test:updateLeaderboards": "ts-node --transpile-only scripts/testUpdateLeaderboards.ts",
    "sanity:wrapReports": "ts-node --transpile-only scripts/sanityWrapReports.ts",
    "sanity:reports": "ts-node --transpile-only scripts/sanityReports.ts",
    "sanity:leaderboards": "ts-node --transpile-only scripts/sanityLeaderboards.ts",
    "test:schedule": "ts-node --transpile-only scripts/testSchedule.ts",
    "sanity:schedule": "ts-node --transpile-only scripts/sanitySchedule.ts",
    "seed:spectator": "ts-node --transpile-only scripts/seedSpectatorData.ts",
    "sanity:spectator": "ts-node --transpile-only scripts/sanitySpectatorData.ts",
    "seed:spectator-overlay": "ts-node --transpile-only scripts/seederSpectator.ts",
    "sanity:spectator-overlay": "ts-node --transpile-only scripts/sanitySpectator.ts",
    "seed:polls": "ts-node --transpile-only scripts/seedPolls.ts",
    "sanity:polls": "ts-node --transpile-only scripts/sanityPolls.ts",
    "seed:clips": "ts-node --transpile-only scripts/seedClips.ts",
    "sanity:clips": "ts-node --transpile-only scripts/sanityClips.ts",
    "export:clips": "ts-node --transpile-only scripts/exportClips.ts",
    "archive:tournament": "ts-node --transpile-only scripts/tournamentWrapUp.ts",
    "security:sanity": "ts-node --transpile-only scripts/securitySanity.ts",
    "seed:admin": "ts-node --transpile-only scripts/seedAdmin.ts",
    "sanity:admin": "ts-node --transpile-only scripts/sanityAdmin.ts",
    "seed:admins": "ts-node --transpile-only scripts/seedAdmins.ts",
    "sanity:admins": "ts-node --transpile-only scripts/sanityAdmins.ts",
    "test:audit": "ts-node --transpile-only scripts/testAuditLogs.ts",
    "seed:tournaments": "ts-node --transpile-only scripts/seedTournaments.ts",
    "sanity:tournaments": "ts-node --transpile-only scripts/sanityTournaments.ts",
    "seed:competitions": "ts-node --transpile-only scripts/seedCompetitions.ts",
    "prune:archived": "ts-node --transpile-only scripts/pruneArchived.ts",
    "seed:sample": "ts-node --transpile-only scripts/seedSampleData.ts",
    "sanity:sample": "ts-node --transpile-only scripts/sanitySampleData.ts"
  },
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@faker-js/faker": "^10.0.0",
    "@types/cors": "^2.8.19",
    "cors": "^2.8.5",
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.0.1"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.12.0",
    "@typescript-eslint/parser": "^5.12.0",
    "eslint": "^8.9.0",
    "eslint-config-google": "^0.14.0",
    "eslint-plugin-import": "^2.25.4",
    "firebase-functions-test": "^3.1.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  },
  "private": true
}
